package days04;

public class Ex03_02 {

	public static void main(String[] args) {
		/*
	         // 1+2
	         conn = DBConn.getConnection();
	         cstmt = conn.prepareCall(sql);

	         String sql = "{ call up_updatedept( pdeptno=>?" ;
	         int index = 1;
	         cstmt.setInt(index++, deptno);  // IN 
	         
	         if (!dname.trim().equals(""))  {
	            sql += ", pdname=>?"; 
	            cstmt.setString(index++, dname);
	         }
	         if (!loc.trim().equals("")) {
	            sql += ", ploc=>?";
	            cstmt.setString(index++, loc);
	         }
	          
	         sql += ")";
	         
	         System.out.println( sql );
	         */

	}

}
